apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-admin
  template:
    metadata:
      labels:
        app: user-admin
    spec:
      containers:
      - name: user-admin
        image: cristianara/user-admin-k8s:v1
        env:
        - name: POSTGRES_URL
          value: "postgresql://adminuser:MiProyecto123@postgres:5432/uce"
        - name: REDIS_URL
          value: "redis://redis:6379"
        ports:
        - containerPort: 6000
---
apiVersion: v1
kind: Service
metadata:
  name: user-admin
spec:
  type: NodePort
  selector:
    app: user-admin
  ports:
  - port: 6000
    targetPort: 6000
    nodePort: 30081
